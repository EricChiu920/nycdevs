import React from 'react';

const styles = {
  frontSize: '20em',
  color: 'black',
  display: 'flex'
}

const InvalidPath = () => {
  return (
    <React.Fragment>
      <p styles={styles}>Invalid Path</p>
    </React.Fragment>
  )
}

export default InvalidPath;
